(define (jmul a b)
  (cond ((= b 0)
         0)
	((even? b)
         ;(jmul (double a) (half b)))
         (double (jmul a (half b))))
        (else
         (+ a (jmul a (sub1 b))))))

(define (double x) (arithmetic-shift x 1))
(define (half x) (arithmetic-shift x -1))


